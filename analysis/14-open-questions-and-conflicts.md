# 14. Open Questions and Unknowns

| ID | Question / unknown | Why it matters | Recommended experiment |
|---|---|---|---|
| Q1 / U1 | PostgreSQL-mode behavioral parity was not runtime-verified (tag filtering, JSON columns, timestamps serialize differently at the driver level). | A replacement claiming PG support could silently diverge. | Run the full §11 vector set against `docker-compose.postgres.yml`. |
| Q2 | There is **no user-facing delete** for `local`/`url` images — is that intended product behavior or a gap? | Determines whether a replacement must add/withhold deletion; affects storage growth. | Product decision; check issue tracker (`bd search delete image`). |
| Q3 | Settings carry `metadataSyncEnabled`, `syncDescription`, `syncCoordinates`, `syncTags`, and docs describe Immich metadata write-back endpoints, but no code path consumes any of it. | Dead config vs. planned feature; replacement must decide whether to keep the settings keys (frontend renders them). | Confirm removal intent (recent commits removed Immich legacy paths); grep history for `sync-metadata`. |
| Q4 | Worker→server WS frames (`plate-solving-update` sent by the worker as a WS *client*) are never re-broadcast by the server — do browsers miss any updates in worker-driven solves? | If yes, replacements must not copy the relay; browser updates rely on the server-side apply path which only runs in-process. In the split-process production topology, worker-applied results may reach browsers only via manual refetch. | With the container running, complete an auto-solve and watch a browser WS: is a `success` frame received? |
| Q5 | Changing `immich.syncFrequency` via the admin API does not reschedule the live cron (reschedule function exists, never called from the settings route). | Users may believe schedule changes apply immediately. | Change frequency to `* * * * *` at runtime; watch whether syncs start within a minute or only after restart. |
| Q6 | No mutual exclusion between concurrent syncs (manual + cron), or sync racing backfill/ingest on the same asset; sync fetches have no timeout. | Duplicate-insert or long-hang behavior under overlap is undefined. | Fire manual sync during a scheduled run against a slow Immich double; inspect for duplicate `(source_type, source_id)` rows. |
| Q7 | URL ingestion has no SSRF protection beyond http/https scheme (private/link-local addresses fetchable) and no content-type/size pre-check before buffering. | Security posture decision for a replacement (app is anonymous!). | Attempt `/api/sources/ingest-url` with `http://169.254.169.254/` in a controlled env. |
| Q8 | The 500 MB `MAX_ORIGINAL_BYTES` upload check buffers the file in memory first; behavior under multi-hundred-MB uploads (memory pressure, request timeouts) unobserved. | Capacity planning for a replacement. | Upload a ~400 MB file; observe memory/latency. |
| Q9 | `plate_solving_jobs.status` schema default is `pending` and stats/worker treat `pending` as in-flight, but no code path creates `pending` jobs — do legacy rows exist in the wild? | Replacement state machines must still accept `pending`. | Query production databases for `status='pending'`. |
| Q10 | Sidecar filename collisions: two images with the same `filename` and month share a sidecar path (last write wins). | Data-loss corner for replacements to fix or reproduce. | Solve two same-named images with sidecars on; inspect output dir. |
| Q11 / U3 | SQLite backup endpoint reads the live file without checkpoint/lock; restore consistency under write load unknown. | Backup guarantees to advertise. | Backup during heavy ingest; integrity-check the copy. |
| Q12 | `sources` status always reports `lastSync: null` and `healthy: true` (hardcoded) — is health intended to be real? | Replacement could implement real health; UI currently expects the field. | Product decision. |
| Q13 / U4 | Concurrent same-image `writeImage` (e.g. backfill racing re-ingest) is explicitly unguarded (source comment: "prevented at the caller level" — no such guard found across processes). | Potential rendition corruption window. | Race backfill vs upload for the same id in a test double. |
| Q14 | E2E suite expectations for seeded data (`?image=1`) imply a seeding flow (`npm run seed`) not analyzed here. | Test-vector parity for a replacement's fixtures. | Inspect `tools/scripts/seed.ts` and align fixtures. |
| U2 | Downgrade behavior after a failed upgrade is undefined (forward-only migrations). | Ops runbook for replacements. | Document as unsupported or add explicit guard. |
| U5 | `docs/features/` describes Sessions, Calibration-file management, Dashboard, NINA plugin, Sky-map overlays — none appear in the runtime surface. | Scope control: these are roadmap docs, not behavior. | Exclude from replication; confirm with maintainer. |

---

# 15. Conflict Log

Contradictions between evidence sources. Resolution rule applied throughout the package: **runtime observation > tests > source > documentation**.

| ID | Conflict | Evidence A | Evidence B | Resolution in this package |
|---|---|---|---|---|
| C1 | `GET /api/admin/cron-jobs` documented as returning job status; actually returns 500 at runtime (live scheduler handles are not JSON-serializable). | DOC-01 (documented) | API-04 (observed 500); SRC-19 (cause) | Documented as broken; replacement should return the serializable fields (AC-11a). |
| C2 | docs/api.md documents `POST /api/immich/sync-metadata/{imageId}` and `/sync-metadata-all`; no such routes exist. | DOC-01 | SRC-05 (router), API probing | Endpoints declared nonexistent; related settings flagged as dead config (Q3). |
| C3 | docs say catalog status returns `objectCount` and browse returns `limit`; runtime returns `count` and `pageSize`. | DOC-01 | API-06, API-09 | Runtime shapes are contractual. |
| C4 | docs stats shape (`plateSolvedImages`, `totalIntegrationHours`, no `uniqueTargets`) vs runtime (`plateSolved`, `totalHours`, `uniqueTargets`). | DOC-01 | API-02 | Runtime shape contractual. |
| C5 | docs image shape includes `immichId`, `thumbnailUrl`, `fullUrl`; runtime records have none (column dropped in migration 0011; URLs derived client-side). | DOC-01 | API-11/13, MIG-01, SCHEMA-01 | Runtime shape contractual; `immichId` retired in favor of `(sourceType, sourceId)`. |
| C6 | docs error format includes an `error` detail field; runtime errors are `{message}` only. | DOC-01 | API-04..12 | `{message}`-only is the contract. |
| C7 | docs claim location edits "sync back to the corresponding Immich asset"; no code path writes to Immich at all. | DOC-01 | SRC-10, SRC-28 (no write calls) | Treated as stale docs (related to removed metadata-sync feature, Q3). |
| C8 | DELETE semantics inconsistent: equipment/group/location/user-target/image-equipment DELETEs return success for nonexistent ids; acquisition DELETE 404s; docs imply uniform 404s. | DOC-01, docs error table | SRC-08/09/10/12/21 (no existence checks), SRC-03 (acquisition checks) | As-built inconsistency documented; replacement must choose knowingly (AC-11c). |
| C9 | docs list env vars `IMMICH_URL`/`IMMICH_API_KEY` as server API behavior defaults; the server config service reads Immich settings only from the DB (those env vars appear only in the worker-standalone example and compose mapping paths). | DOC-01, DOC-04 | SRC-17 (no env fallback for Immich), SRC-31 | Admin-settings-only for the server; env vars affect worker standalone mode only. |
| C10 | CLAUDE.md says sync schedules etc. configured in admin UI "take precedence over environment variables," implying live effect; observed gap: sync cron changes need restart. | DOC-02 | SRC-04/19 (no reschedule call) | Precedence claim true for values; scheduling liveness gap logged (Q5). |
| C11 | Legacy schema module (`schemas/schema.ts`) still describes the pre-`sourceType` image shape (with `tags` as PG array, no `targetName`), diverging from the live schemas. | SCHEMA-03 | SCHEMA-01/02, MIG-01 | Legacy module ignored as evidence of current behavior. |
| C12 | Working tree vs HEAD: `db.ts` catch-block removal changes the SQLite-init failure message (HEAD masks causes as "No database configuration found"). | git diff (uncommitted) | SRC-02 at HEAD | Package describes HEAD behavior; noted in Scope. |
| C13 | `plate_solving_jobs.status` default `pending` (schema) vs jobs always created as `processing` (code); docs list `pending` as a normal lifecycle state. | SCHEMA-01, DOC-01 | SRC-18 | `processing→success|failed` is the live lifecycle; `pending` retained for legacy tolerance (Q9). |
| C14 | E2E test asserts GitHub header link `https://github.com/mstelz/sidereal`; docs/api.md support section links `mstelz/Sidereal` (case) — trivially consistent, but repo hosting may have moved post-rename (MIGRATION.md describes Skymmich rename). | TEST-05 | DOC-01/03 | Header URL kept as the UI contract. |
